# Node values are created when it is initialized

class Node
    #assuming that we have a class 'boardstate', I am pushing all methods for
    #analyzing the board (possible moves and such) onto that class
    def __init__(self, boardState):
        self.boardState = boardState     #The current boardstate is the node
        #self.parent = parent             #Where the node came from (prob don't need)
        self.children = boardState.moves #List of possible boardstates that are 1 move away
        self.value = boardState.evaluate #The value of the current boardstate
        return
